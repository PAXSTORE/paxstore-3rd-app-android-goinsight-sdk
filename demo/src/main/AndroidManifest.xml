<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required on Android 11+ to get full installed app list; otherwise only self and <queries> packages are visible -->

    <application
        android:name=".base.BaseApplication"
        android:icon="@mipmap/logo_demo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.GoInsightActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add below meta-data to support GoInsight -->
        <meta-data
            android:name="PAXVAS_Insight"
            android:value="true" />
    </application>
</manifest>